anaconda_config_version: 1
upstream_sources:
  - pypi:
      identifier: cfgrib
      strip_prefix: v
    packages: [cfgrib]
  - github:
      identifier: ecmwf/cfgrib
      strip_prefix: v
    packages: [cfgrib]
